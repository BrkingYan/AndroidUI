package com.example.yy.ui;

import android.content.Context;
import android.graphics.Canvas;
import android.util.AttributeSet;
import android.view.View;

public class RopeDialView extends View {

    private static final String TAG = "ropeDialView";

    public RopeDialView(Context context,  AttributeSet attrs) {
        super(context, attrs);

    }

    @Override
    protected void onDraw(Canvas canvas) {
        super.onDraw(canvas);
        //TODO 旋转船体

    }
}
